// src/components/TopBar/TimerWidget.tsx
import React from 'react';

export default function TimerWidget() {
  // Placeholder: Replace with real timer logic and API integration
  // Should show running timer, start/stop, and link to time entries
  return (
    <div className="flex items-center gap-2 px-3 py-1 border rounded bg-gray-50 text-xs text-gray-700">
      <span>⏱️ Timer</span>
      {/* Add timer state here */}
    </div>
  );
}
